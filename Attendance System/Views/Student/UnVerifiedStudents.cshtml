@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model List<User>

<h1>UnVerified Students</h1>
@*
<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p> *@
<table class="table">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.Name
                </td>
                <td>
                    <a asp-controller="Student" asp-action="VerifyStudent" asp-route-id="@item.ID" class="btn btn-primary">Verify</a>
                    @* <a asp-controller="Student" asp-action="VerifyStudentForm" asp-route-id="@item.ID">Verify</a> | *@
                    @* <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a> *@
                </td>
            </tr>
        }
    </tbody>
</table>
